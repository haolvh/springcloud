server:
  port: 8050
spring:
  application:
    name: feign
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
#feign集成的熔断机制 是否开启熔断器
feign:
  hystrix:
    enabled: true