server:
  port: 8030
spring:
  application:
    name: gateway
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

#zuul.routes.provider: 给服务提供者provider来设置映射
zuul:
  routes:
    provider: /p/**
